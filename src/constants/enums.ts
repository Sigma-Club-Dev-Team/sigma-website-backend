export enum Role {
  SuperAdmin = 'super-admin',
  QuizMaster = 'quiz-master',
  Adhoc = 'adhoc',
}

export enum QuizStatus {
  Pending = 'pending',
  InProgress = "in-progress",
  Completed = "completed"
}